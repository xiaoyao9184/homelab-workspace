---

- name: deploy
  vars:
    run_wsl: |
      ansible-playbook \
        --inventory $PWD/ansible \
        $PWD/../../../docker/stack-standalone/gitlab/gitlab-ce/ansible-playbook.deploy.yml
  hosts: docker-manager
  tasks: 

    - name: set ws_dir
      include_tasks: "{{ playbook_dir }}/../../../../.seed/ansible-tasks/ws-dir.set.yml"

    - name: set dir_name
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/dir-name.set.yml"
      vars:
        name:
          high: 2

    - name: copy dir_path
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/dir-path.copy.yml"

    - name: add external_sec
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/docker-secrets.add.yml"
      vars:
        secrets: "{{ gitlab.sec }}"

    - name: copy file for cfg
      ansible.builtin.copy: "{{ item }}"
      loop: "{{ gitlab.cfg_copy }}"

    - name: add external_cfg
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/docker-configs.add.yml"
      vars:
        configs: "{{ gitlab.cfg }}"

    - name: add external_vol
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/docker-volumes.add.yml"
      vars:
        volumes: "{{ gitlab.vol }}"

    - name: copy file for vol
      ansible.builtin.copy: "{{ item }}"
      loop: "{{ gitlab.vol_copy }}"

    - name: add external_net
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/docker-networks.add.yml"
      vars:
        networks:
          external_net-gitlab:
            driver: overlay
            attachable: true

    - name: build env_file
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/docker-env.create.yml"
      vars:
        env: 
          dest: "{{ dir_path }}/external_env-all.env"
          data: "{{ gitlab.env }}"

    - name: add stack service
      include_tasks: "{{ ws_dir }}/.seed/ansible-tasks/docker-stack.add.yml"
      vars:
        stack_name: "{{ dir_name[0] }}"
        stack_path: "{{ dir_path }}"
