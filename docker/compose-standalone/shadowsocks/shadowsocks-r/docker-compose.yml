version: "3.8"

services:
  shadowsocks-r:
    image: teddysun/shadowsocks-r:latest
    env_file: external_env-r.env
    volumes:
      - ./../config/r.json:/etc/shadowsocks-r/config.json
    network_mode: "host"
    container_name: shadowsocks-r
    restart: always