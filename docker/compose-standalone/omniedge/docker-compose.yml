version: "3.8"

services:
  omniedge:
    image: omniedge/omniedge:latest
    env_file: external_env-all.env
    network_mode: "host"
    container_name: omniedge
    restart: always
    privileged: true