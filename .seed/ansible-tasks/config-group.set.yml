---


- name: config-group.set|set config_group
  ansible.builtin.set_fact:
    "{{ group.register }}": "{{ group.all[group.name] | default([group.name]) }}"

- ansible.builtin.debug:
    verbosity: 3
    var: "{{ group.register }}"