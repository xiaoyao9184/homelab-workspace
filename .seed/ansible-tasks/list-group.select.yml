---


- name: list-group.select|set select group.register
  vars:
    all: "{{ group.data.values() | flatten(1) }}"
  ansible.builtin.set_fact:
    "{{ group.register }}": "{{ group.data[group.select] | default(all) }}"

- debug: msg="{{ lookup('vars', group.register) }}" verbosity=3
