---

- name: set ws_path
  ansible.builtin.set_fact:
    ws_path: "{{ path | realpath }}"

- name: print ws_path
  ansible.builtin.debug:
    msg: 
      - "ws {{ ws_path }}"
      - "from {{ playbook_dir }}"