all:
  hosts:
    localhost:
  vars:
    ansible_connection: local

    remote:
      # workspace: "~/.seed/remote_homelab" 
  children:
    windows:
      hosts:
        localhost:

    wsl:
      hosts:
        localhost:

    linux:
      hosts:
        localhost:

    docker:
      hosts:
        localhost:

    docker-manager:
      hosts:
        localhost:

    docker-openwrt-node:
      hosts:
        localhost:
 