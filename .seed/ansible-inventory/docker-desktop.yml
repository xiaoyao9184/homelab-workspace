all:
  hosts:
    localhost:
  vars:
    ansible_connection: local

    workspace:
      remote: "~/.seed/remote_homelab" 
  children:
    remote:
      hosts:
        localhost:

    windows:
      hosts:
        localhost:

    wsl:
      hosts:
        localhost:

    linux:
      hosts:
        localhost:

    docker:
      hosts:
        localhost:

    docker-manager:
      hosts:
        localhost:

    docker-openwrt-node:
      hosts:
        localhost:
 